# Быстрое возведение в степень

Реализуйте функцию $`\mathrm{BinPow(a,\ b,\ c)}`$ которая вычисляет $`\mathrm{a^b\ (mod\ c)}`$.
Количество операций должно быть пропорционально двоичному логарифму $`\mathrm{b}`$.
